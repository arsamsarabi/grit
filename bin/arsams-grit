#!/usr/bin/env bash
# npm rejects .ts bin paths; this wrapper runs the Bun entrypoint.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
if ! command -v bun >/dev/null 2>&1; then
  echo "arsams-grit requires Bun. Install: https://bun.sh" >&2
  exit 1
fi
exec bun "$ROOT/src/index.ts" "$@"
